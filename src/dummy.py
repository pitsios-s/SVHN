"""
=======================================
A simple plot with a custom dashed line
=======================================

A Line object's ``set_dashes`` method allows you to specify dashes with
a series of on/off lengths (in points).
"""
import numpy as np
import matplotlib.pyplot as plt

# plt.style.use("ggplot")
# x = np.asarray([1, 2, 3, 4, 5])
# y = np.asarray([5, 10, 15, 20, 25])
# y2 = np.asarray([10, 20, 30, 40, 50])
#
# fig, ax = plt.subplots(2, 2, sharex='col')
# ax[0, 0].plot(x, y, '-', linewidth=1, color="darkgreen")
# ax[0, 1].plot(x, y2, '-', linewidth=1, color="black")
#
# for i in x:
#     ax[0, 0].axvline(x=i, ymin=0, ymax=25, linewidth=2, color="darkorange", label=None)
#
#
# plt.show()


